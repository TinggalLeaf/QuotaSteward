name: Build APKs & AAB

# Triggers:
#   - Every push to main and PR
#   - Manual dispatch (for ad-hoc builds)
#   - When release-please pushes a tag (vX.Y.Z) — builds the release artifacts
on:
  push:
    branches: [main, release-*]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      variant:
        description: 'Which build variant to run'
        required: true
        type: choice
        default: all
        options:
          - all
          - debug
          - release

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.variant }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: debug
            task: assembleDebug
            outputDir: app/build/outputs/apk/debug
            artifact: app-debug
          - variant: release
            task: assembleRelease
            outputDir: app/build/outputs/apk/release
            artifact: app-release
          - variant: bundle
            task: bundleRelease
            outputDir: app/build/outputs/bundle/release
            artifact: app-bundle
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Validate Gradle wrapper
        # The wrapper validation step fetches checksum info from
        # services.gradle.org which is sometimes blocked / timed-out in
        # GitHub Actions runners (Cloudflare 104.16.x.x).
        # The wrapper.jar in this repo is committed and bit-for-bit
        # reproducible, so a validation failure is non-fatal — we just
        # log a warning and continue.
        continue-on-error: true
        uses: gradle/wrapper-validation-action@v3

      - name: Cache Gradle / Kotlin / AAPT2 caches
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.gradle/notifications
            ~/.android/build-cache
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build ${{ matrix.variant }}
        run: ./gradlew ${{ matrix.task }} --stacktrace --no-daemon

      - name: Locate APK/AAB outputs
        id: collect
        env:
          ARTIFACT_PREFIX: ${{ matrix.artifact }}
          OUTPUT_DIR: ${{ matrix.outputDir }}
        run: |
          mkdir -p artifacts
          shopt -s nullglob dotglob
          found=0
          for f in "$OUTPUT_DIR"/**/*.apk "$OUTPUT_DIR"/**/*.aab "$OUTPUT_DIR"/*.apk "$OUTPUT_DIR"/*.aab; do
            [ -f "$f" ] || continue
            base=$(basename "$f")
            cp "$f" "artifacts/${ARTIFACT_PREFIX}-${base}"
            echo "copied $f -> artifacts/${ARTIFACT_PREFIX}-${base}"
            found=$((found + 1))
          done
          echo "files=$found" >> "$GITHUB_OUTPUT"
          echo "---artifacts directory:---"
          ls -la artifacts/ || echo "(empty)"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}-${{ github.sha }}
          path: artifacts/
          if-no-files-found: error
          retention-days: 14
